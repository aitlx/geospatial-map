export default function CropRecommendationConfiguration() {
  return (
    <section className="px-4 py-6 text-slate-800 sm:px-6 lg:px-8">
      <div className="mx-auto max-w-4xl space-y-6 rounded-3xl border border-emerald-200 bg-white p-8 shadow-sm">
        <h1 className="text-2xl font-semibold uppercase tracking-[0.08em] text-emerald-800">Crop Recommendation Configuration</h1>
        <p className="text-sm text-slate-500">
          Govern the heuristics and data pipelines that power intelligent crop suggestions. Parameter tuning and simulation
          controls will activate here to keep recommendations grounded in local realities.
        </p>
        <div className="rounded-2xl border border-slate-200 bg-slate-50 p-6 text-sm text-slate-600">
          Configuration tooling is temporarily disabled while the recommendation system is being finalized. Return once the
          engine is aligned with production data.
        </div>
      </div>
    </section>
  )
}
